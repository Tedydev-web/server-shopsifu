# ===========================================================================
# Cấu hình môi trường ứng dụng
# ===========================================================================
# Môi trường chạy ứng dụng (development, production, staging)
NODE_ENV=
# Tên miền cho cookie (bỏ comment và cấu hình khi deploy)
COOKIE_ROOT_DOMAIN=
# Cấu hình production
API_HOST_URL=
API_LOCAL_URL=
FRONTEND_HOST_URL=
FRONTEND_LOCAL_URL=
PORT=
# Tên ứng dụng
APP_NAME=Shopsifu
# ===========================================================================
# Cấu hình cơ sở dữ liệu
# ===========================================================================
# URL kết nối đến PostgreSQL (bao gồm host, port, database name, schema)
DATABASE_URL=
# ===========================================================================
# Cấu hình xác thực (Authentication)
# ===========================================================================
ACCESS_TOKEN_EXPIRES_IN=
REFRESH_TOKEN_EXPIRES_IN=
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
LOGIN_SESSION_TOKEN_EXPIRES_IN=
OTP_TOKEN_EXPIRES_IN=
REMEMBER_ME_REFRESH_TOKEN_EXPIRES_IN=
COOKIE_SECRET=
CSRF_SECRET=
SECRET_API_KEY=
# ===========================================================================
# Cấu hình thông tin admin
# ===========================================================================
ADMIN_NAME=
ADMIN_PASSWORD=
ADMIN_EMAIL=
ADMIN_PHONE_NUMBER=
# ===========================================================================
# Cấu hình tích hợp bên thứ ba
# ===========================================================================
RESEND_API_KEY=
# ===========================================================================
# Cấu hình xác thực Google OAuth
# ===========================================================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
GOOGLE_CLIENT_REDIRECT_URI=