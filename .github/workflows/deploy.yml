name: 🚀 Deploy to Production

on:
  workflow_run:
    workflows: ['🏗️ Build Docker Image']
    types: [completed]
    branches: [master] # Only master branch for production
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Image tag to deploy (optional)'
        required: false
        default: 'latest'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: tedydev-web/server-shopsifu
  VPS_HOST: ${{ secrets.VPS_HOST }}
  VPS_USERNAME: ${{ secrets.VPS_USERNAME }}
  VPS_PATH: ${{ secrets.VPS_PATH }}

jobs:
  deploy:
    name: 🚀 Deploy Application
    runs-on: ubuntu-latest
    timeout-minutes: 15 # Reduced timeout for faster feedback
    # Only run if build workflow succeeded OR manual dispatch
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    outputs:
      deploy-time: ${{ steps.deploy-time.outputs.deploy-time }}
      deploy-status: ${{ job.status }}
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ⏱️ Start deploy timer
        id: deploy-timer
        run: echo "start_time=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: 🔍 Validate deployment inputs
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Manual deployment triggered"
            echo "Image tag: ${{ github.event.inputs.image_tag }}"
          else
            echo "Automatic deployment from build workflow"
          fi

      - name: 🐳 Deploy with Docker Compose
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ env.VPS_HOST }}
          username: ${{ env.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          timeout: 60s
          command_timeout: 300s
          script: |
            set -euo pipefail

            # Debug environment variables
            echo "🔍 Debug: Current working directory: $(pwd)"
            echo "🔍 Debug: VPS_PATH from env: ${{ env.VPS_PATH }}"
            echo "🔍 Debug: VPS_PATH from secrets: ${{ secrets.VPS_PATH }}"

            # Set VPS_PATH explicitly
            VPS_PATH="${VPS_PATH:-workspace/shopsifu/server-shopsifu}"
            echo "🔍 Debug: Final VPS_PATH: $VPS_PATH"

            # Change to correct directory
            cd "$VPS_PATH" || {
              echo "❌ Failed to change to directory: $VPS_PATH"
              echo "📁 Available directories:"
              ls -la
              exit 1
            }

            echo "✅ Changed to directory: $(pwd)"
            echo "📁 Directory contents:"
            ls -la

            echo "🚀 Starting deployment process..."

            # Login to GHCR
            echo '${{ secrets.GITHUB_TOKEN }}' | docker login ghcr.io -u '${{ github.actor }}' --password-stdin

            # Stop existing services
            echo "🛑 Stopping existing services..."
            docker compose down

            # Pull latest images from GHCR
            echo "📥 Pulling latest images from GitHub Container Registry..."
            docker compose pull

            # Deploy with Docker Compose
            echo "🚀 Deploying with Docker Compose..."
            docker compose up -d

            # Verify deployment with better health check
            echo "🔍 Verifying deployment..."
            echo "⏳ Waiting for services to start..."

            # Wait for services to be ready
            for i in {1..10}; do
              if docker compose ps | grep -q "shopsifu_server.*Up"; then
                echo "✅ Service is running (attempt $i/10)"
                break
              fi
              echo "⏳ Service not ready yet (attempt $i/10)..."
              sleep 3
            done

            # Final verification
            if ! docker compose ps | grep -q "shopsifu_server.*Up"; then
              echo "❌ Deployment verification failed after 10 attempts"
              echo "📊 Current service status:"
              docker compose ps
              exit 1
            fi
            echo "✅ Deployment verification passed"

            # Show service status
            echo "📊 Service status:"
            docker compose ps

      - name: 📊 Calculate deploy time
        id: deploy-time
        run: |
          DEPLOY_END=$(date +%s)
          DEPLOY_START=${{ steps.deploy-timer.outputs.start_time }}
          DEPLOY_DURATION=$((DEPLOY_END - DEPLOY_START))
          echo "deploy-time=$DEPLOY_DURATION" >> "$GITHUB_OUTPUT"
          echo "⏱️ Deploy completed in ${DEPLOY_DURATION}s"

  